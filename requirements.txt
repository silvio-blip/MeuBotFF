# Dependências do bot S.art
# Bot do Discord para Free Fire com verificação por radar, ranking, torneios e painel admin

# Discord
discord.py>=2.3.0          # Biblioteca principal do bot

# Banco de dados
supabase>=2.0.0             # Cliente Supabase para Python

# HTTP / API
aiohttp>=3.9.0              # Cliente HTTP assíncrono usado pelo bot e pela API FF

# Variáveis de ambiente
python-dotenv>=1.0.0        # Carrega variáveis do arquivo .env

# Processamento de imagem
Pillow>=10.0.0               # Geração de cards de perfil (imagine, fundo, etc.)

# Música
# wavelink removido — mantém yt-dlp + FFmpegPCMAudio (ver scripts/musica_setup.sql)

# Audio/FFmpeg
ffmpeg-python                # Para reprodução de áudio
yt-dlp                       # Download de streams de YouTube/SoundCloud
